[pytest]
testpaths = tests
addopts =
    --verbose
    --tb=short
    --capture=no
    -rfEsxX
    -K
asyncio_mode = auto
markers =
    auth
    basic
    blocking
    raw
    cluster
    resp2
    nokeydb
    keydb
    nodragonfly
    dragonfly
    sentinel
    stack
    ssl
    uds
    nocluster
    noresp3
    clusteronly
    cached
    min_python
    min_server_version
    max_server_version
    os
    nopypy
    pypyonly
env =
    D:COREDIS_RUNTIME_CHECKS=true
