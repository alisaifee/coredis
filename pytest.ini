[pytest]
testpaths = tests
addopts =
    --verbose
    --tb=short
    --capture=no
    -rfEsxX
    -K
    --cov=coredis
asyncio_mode = auto
markers =
    nohiredis
    nocluster
    noresp3
    clusteronly
    min_server_version
    max_server_version
    os
env =
    COREDIS_RUNTIME_CHECKS=true
